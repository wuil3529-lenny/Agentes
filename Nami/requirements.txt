# === Luffy Agent - Dependencias ===
# Arquitectura: LangGraph + langchain_ollama + Ollama (Python 3.14 compatible)

# Orquestación de agentes (moderno, robusto, Python 3.14 compatible)
langgraph>=1.2.0

# Integración con Ollama (paquete independiente actualizado)
langchain-ollama>=1.1.0

# Core de LangChain (mensajes, prompts, etc.)
langchain-core>=1.0.0
langchain>=1.3.0

# Variables de entorno
python-dotenv>=1.0.0

# Consola enriquecida
rich>=13.7.0

# Comunicación HTTP (para llamadas opcionales directas a Ollama REST API)
requests>=2.31.0
